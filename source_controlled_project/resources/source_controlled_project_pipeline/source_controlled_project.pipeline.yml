resources:
  pipelines:
    source_controlled_project_pipeline:
      name: source_controlled_project_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
